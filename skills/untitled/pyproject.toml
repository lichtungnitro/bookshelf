# Ruff configuration for scripts under this folder (not an installable package).
# Runtime scripts use only the Python standard library.

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
